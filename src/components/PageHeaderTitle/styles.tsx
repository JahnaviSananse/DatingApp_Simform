/* REACT */
import {StyleSheet} from 'react-native';

import {getHeightWithScaleFactor} from '../../utils/dimensions';

export default StyleSheet.create({
  marginBottomSubTitle: {
    marginBottom: getHeightWithScaleFactor(15),
  },
});
